<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/wipro_training</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Kimtae@7</property>

        <!-- JDBC connection pool (use built-in) -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- SQL Dialect for MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Show SQL queries in console -->
        <property name="hibernate.show_sql">true</property>

     

        <!-- Automatically create/update database tables -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping your entity classes -->
        <!-- Add all annotated classes here -->
        <mapping class="com.wipro.Entity.Pan" />

    </session-factory>
</hibernate-configuration>
